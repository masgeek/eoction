/* eslint-disable no-console,no-undef,no-unused-vars */


function evaluateProducts(product_id) {

    var $spinnerValue = $('#quantity-' + product_id).val();

    console.log('Action started for ' + $spinnerValue);
}